name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore Polymarket.Client.slnx

      - name: Build
        run: dotnet build Polymarket.Client.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test tests\Polymarket.Client.Tests\Polymarket.Client.Tests.csproj --configuration Release --no-build
